// eslint-disable-next-line max-lines-per-function
module.exports = (start) => [
  start.filter((s) => s === 'A').length,
  start.filter((s) => s === 'B').length,
  start.filter((s) => s === 'C').length,
  start.filter((s) => s === 'D').length,
  start.filter((s) => s === 'E').length,
  start.filter((s) => s === 'F').length,
  start.filter((s) => s === 'G').length,
  start.filter((s) => s === 'H').length,
  start.filter((s) => s === 'I').length,
  start.filter((s) => s === 'J').length,
  start.filter((s) => s === 'K').length,
  start.filter((s) => s === 'L').length,
  start.filter((s) => s === 'M').length,
  start.filter((s) => s === 'N').length,
  start.filter((s) => s === 'O').length,
  start.filter((s) => s === 'P').length,
  start.filter((s) => s === 'Q').length,
  start.filter((s) => s === 'R').length,
  start.filter((s) => s === 'S').length,
  start.filter((s) => s === 'T').length,
  start.filter((s) => s === 'U').length,
  start.filter((s) => s === 'V').length,
  start.filter((s) => s === 'W').length,
  start.filter((s) => s === 'X').length,
  start.filter((s) => s === 'Y').length,
  start.filter((s) => s === 'Z').length,
];
